export type FooterProps = {}


export const Footer = ({

}: FooterProps) => {
return (
    <footer className="w-full px-8 py-4 bg-primary text-primary-foreground">
        <p className="text-xs">&copy; ACOSA. Work in Progress.</p>
    </footer>
)
}